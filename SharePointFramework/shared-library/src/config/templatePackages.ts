/**
 * Folder under the hub Template Library (Malbibliotek) where the template
 * package catalog stores imported packages' project-level provisioning assets
 * (template/extension/content JSON) for the setup wizard. The name is
 * reserved: the document template selector excludes this folder and
 * everything under it, since its files are wizard inputs — not document
 * templates.
 */
export const TEMPLATE_PACKAGE_STORE_FOLDER = 'pp-packages'
