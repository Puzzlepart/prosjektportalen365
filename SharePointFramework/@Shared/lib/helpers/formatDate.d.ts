/**
 * Format date
 *
 * @param {string} dateStr Date string
 * @param {string} fallback Fallback if date is invalid
 * @param {string} locale Locale
 */
export default function formatDate(dateStr: string, includeTime?: boolean, fallback?: string, locale?: string): string;
