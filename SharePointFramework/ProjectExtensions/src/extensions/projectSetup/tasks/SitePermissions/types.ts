export interface IPermissionConfiguration {
  /**
   * SharePoint group name
   */
  groupName: string

  /**
   * Defines the SharePoint permission level for the project site.
   */
  permissionLevel: string
}
